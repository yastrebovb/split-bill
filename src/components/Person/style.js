import styled from 'styled-components'

export const PersonStyled = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  flex: 1 1 0;
  border-bottom: 2px solid rgba(209, 216, 224, 0.41);
`

export const PersonId = styled.div`
  margin-bottom: 6px;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: -0.25px;
  color: #4e4e4e;
`

export const PersonAmount = styled.div`
  font-size: 28px;
  font-weight: 700;
  color: #4e4e4e;
`
