const splitReducerDefaultState = {
  total: undefined,
  bill: undefined,
  friends: undefined,
  tips: undefined
}

export default (state = splitReducerDefaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
